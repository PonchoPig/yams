# Security policy

Report suspected vulnerabilities using GitHub's private vulnerability
reporting: open the repository's Security tab and select "Report a
vulnerability".

Never open a public issue for a suspected vulnerability.

Never include real memory contents, credentials, query history, or databases
in a report. Use a minimal fictional reproduction.

The latest release and the `main` branch are supported.
